/// Backwards-compat re-export. `BlinkingCursor` lives alongside the rest of
/// the cursor family in [cursors.dart]. Prefer importing the public barrel:
/// `package:streaming_markdown/streaming_markdown.dart`.
library;

export 'cursors.dart' show BlinkingCursor;
