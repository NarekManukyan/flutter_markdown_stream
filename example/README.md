# streaming_markdown_example

Example app for the [`streaming_markdown`](../) Flutter package.

The app has two tabs:

- **Stream demo** — press `Stream` to watch a sample LLM response render
  progressively, with your choice of typing-cursor at the tail.
- **Cursor gallery** — preview every cursor widget the package ships.

Run with:

```bash
flutter run
```
